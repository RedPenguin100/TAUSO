name: tauso

channels:
  - conda-forge
  - bioconda
dependencies:
  - jupyterlab
  - ipykernel
  - uv
  - conda-build
  - conda-lock
  - pip:
    - -e ".[dev]"  # installs tauso + all runtime + dev extras
