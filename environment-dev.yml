name: tauso

channels:
  - conda-forge
  - bioconda
dependencies:
  - python=3.12
  - ruff==0.15.12
  - jupyterlab
  - ipykernel
  - cupy
  - graphviz
  - uv
  - pip:
    - -e ".[dev]"  # installs tauso + all runtime + dev extras
