name: tauso
channels:
  - conda-forge
dependencies:
  - ruff
  - shap
  - jupyterlab
  - matplotlib
  - seaborn
  - ipykernel
  - optuna
  - pip
  - pip:
    - -e .  # <--- CRITICAL: Installs your 'src' in editable mode
    - ruff
  # tooling
  - pytest
  - pytest-regressions
  - pytest-xdist
  - conda-build
  - conda-lock


