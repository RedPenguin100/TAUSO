name: tauso
channels:
  - conda-forge
dependencies:
  - jupyterlab
  - matplotlib
  - seaborn
  - ipykernel
  - pip
  - pip:
    - -e .  # <--- CRITICAL: Installs your 'src' in editable mode

  # tooling
  - pytest
  - pytest-regressions
  - conda-build


