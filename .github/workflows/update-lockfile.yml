name: Update conda lockfile

on:
  schedule:
    - cron: '0 3 * * *'  # 3:00 AM UTC, same window as CI
  workflow_dispatch:      # allow manual trigger

jobs:
  update-lockfile:
    runs-on: ubuntu-latest

    defaults:
      run:
        shell: bash -el {0}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up micromamba
        uses: mamba-org/setup-micromamba@v1
        with:
          environment-name: lock-env
          create-args: >-
            conda-lock
            micromamba
          cache-downloads: true

      - name: Regenerate lockfiles
        run: |
          conda-lock lock \
            --file environment.yml \
            --platform linux-64 \
            --kind explicit \
            --micromamba

      - name: Commit updated lockfile if changed
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          if git diff --quiet conda-linux-64.lock; then
            echo "Lockfile unchanged, nothing to commit."
          else
            git add conda-linux-64.lock
            git commit -m "chore: update conda-linux-64.lock [skip ci]"
            git push
          fi
