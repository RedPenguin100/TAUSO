# Normalize text files to LF in the index, regardless of local autocrlf settings,
# so commits/checkouts don't show line-ending churn (e.g. on documentation/feature_run/build_env.sh).
* text=auto eol=lf
